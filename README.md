# I'm Here

Project developed on Ignite, a React Native bootcamp by Rocketseat.

A simple presence list for an event.

## Run locally
* Clone the repository
`git clone https://github.com/marocss/imhere.git`
* Install dependencies
`npx npm i`
* Run the application
`npx expo start`
